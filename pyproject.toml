[project]
name = "wellai-bot-ai-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "grpcio>=1.80.0",
    "grpcio-reflection>=1.80.0",
    "grpcio-tools>=1.80.0",
    "loguru>=0.7.3",
    "openai>=1.80.0",
    "proto",
    "python-dotenv>=1.2.2",
]

[tool.uv.sources]
proto = { path = "proto" }
